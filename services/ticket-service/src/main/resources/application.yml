server:
  port: 8081

spring:
  application:
    name: ticket-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
