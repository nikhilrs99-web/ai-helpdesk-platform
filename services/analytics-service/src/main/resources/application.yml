server:
  port: 8085

spring:
  application:
    name: analytics-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
